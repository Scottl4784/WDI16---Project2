<h1>Series</h1>
    <button class="waves-effect waves-light btn-small">
        <a class="unstyled-links" href="/users/{{userId}}/series/new">Add a Series</a>
    </button>
<ul>
    {{#each seriesList}}
    <li>
        <a href="/users/{{../userId._id}}/series/{{_id}}/comics"><h2>{{title}}</h2></a>  <p>{{description}}</p>
    </li>
    <form action="/users/{{../userId._id}}/series/{{_id}}?_method=DELETE" method="POST">
        <button type="submit" class="waves-effect waves-light btn-small">Delete Series</button>
    </form>
    <a href="/users/{{../userId._id}}/series/{{_id}}/edit"><button class="waves-effect waves-light btn-small">Edit Series</button></a>
    {{/each}}
</ul>